import React from 'react';
import {View, Text} from 'react-native';

const Details = () => {
    return (
        <view>
            <Text>Details</Text>
        </view>
    );
};

export default Details;